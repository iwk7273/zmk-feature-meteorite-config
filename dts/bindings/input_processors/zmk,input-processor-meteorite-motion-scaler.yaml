# SPDX-License-Identifier: MIT

description: Meteorite input processor that scales relative pointer motion.

compatible: "zmk,input-processor-meteorite-motion-scaler"

include: ip_zero_param.yaml

properties:
  custom-config-scaling:
    type: int
    required: false
    description: 0 uses cursor scaling, 1 uses scroll scaling when CONFIG_ZMK_CUSTOM_CONFIG is enabled.
  scaling-mode:
    type: int
    required: false
    description: 0 disables scaling, 1 enables scaling when CONFIG_ZMK_CUSTOM_CONFIG is disabled.
  max-output:
    type: int
    required: false
    description: Maximum absolute output value.
  half-input:
    type: int
    required: false
    description: Half-input value used for r=abs(input)/half-input.
  exponent-tenths:
    type: int
    required: false
    description: Exponent with 0.1 resolution.
